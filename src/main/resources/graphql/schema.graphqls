type Query {
    books: [Book]
    bookById(id: ID): Book
    version: String
}

type Book {
    id: ID
    title: String
    author: String
}

type Mutation {
    addBook(title: String, author: String): Book
}
